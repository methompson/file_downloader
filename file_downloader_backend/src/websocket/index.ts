/**
 * TODO WebSocket data feed for current downloads
 * Need to set up regular updates to clients about current download speeds, progress, etc.
 * Need to isolate websocket sessions per user so that users only get updates about their own downloads.
 */
